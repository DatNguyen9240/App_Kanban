# ==============================================================
# CẤU HÌNH SERVER VÀ DATABASE (POSTGRESQL / SQLITE)
# ==============================================================

PORT=8080
GIN_MODE=release

# --------------------------------------------------------------
# 1. CẤU HÌNH POSTGRESQL TRÊN RAILWAY
# --------------------------------------------------------------
DB_TYPE=postgres

# URL kết nối nội bộ trên Railway (Dùng cho service Web chạy trên Railway):
DATABASE_URL=postgresql://postgres:PGJZkFTtNwEFYKqeUkuHIXBiqUgGlzZQ@postgres.railway.internal:5432/railway

# URL kết nối từ máy tính cá nhân ở nhà vào Railway Postgres (Public Network):
# (Nếu muốn kết nối từ máy bạn, hãy vào Postgres -> Settings -> Networking -> Enable Public Networking để lấy host:port công khai)
# DATABASE_URL=postgresql://postgres:PGJZkFTtNwEFYKqeUkuHIXBiqUgGlzZQ@<host>.proxy.rlwy.net:<port>/railway

# --------------------------------------------------------------
# 2. BẢO MẬT & XÁC THỰC JWT
# --------------------------------------------------------------
JWT_SECRET=super-secret-kanban-jwt-key-2026
